class CreateUserProp():

    create_user_new_frame_xpath="//iframe[@title='New User ~ Salesforce - Developer Edition']"
    firstname_id="name_firstName"

    # create method here and perform that logic here itself
    # and simply call this method in the UnitTest
    # so the point out over here if we want to use the methods means we should have driver isinstance(with that
    # isnatnce we can see various methods)
    # so thats the reason for this pagefactory we will create a constuctor in which we will passon the driver instance
    # and using that instance we will try to complete those emthod